/** Main content width; headers/footer backgrounds stay full-bleed outside this shell. */
export const STORE_SHELL =
  "mx-auto w-full max-w-[1440px] px-4 sm:px-6 lg:px-8";

/**
 * Fluid product grid (see `.product-grid-fluid` in globals.css):
 * 2 columns on small phones; `repeat(auto-fill, minmax(250px, 1fr))` from sm+.
 */
export const PRODUCT_GRID_COLS = "product-grid-fluid";

/** Alias for product listing grids. */
export const PRODUCT_GRID = "product-grid-fluid";
