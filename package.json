{
  "name": "ecommerce-monorepo",
  "private": true,
  "engines": {
    "node": "20.x || 24.x"
  },
  "description": "Next.js storefront (web) + Express API (server) + optional Firebase Functions",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "dev:stack": "concurrently -n web,api -c cyan,magenta \"npm run dev --prefix web\" \"npm run dev --prefix server\"",
    "dev:web": "npm run dev --prefix web",
    "dev:api": "npm run dev --prefix server",
    "build": "npm ci --prefix web && npm run build --prefix web",
    "build:all": "npm ci --prefix web && npm run build --prefix web && npm run verify:server",
    "start:web": "npm run start --prefix web",
    "start:api": "npm run start --prefix server",
    "stack:health": "node scripts/stack-health.mjs",
    "docker:up": "docker compose -f docker-compose.prod.yml up -d --build",
    "docker:down": "docker compose -f docker-compose.prod.yml down",
    "lint": "npm run lint --prefix web",
    "install:all": "npm install --prefix web && npm install --prefix server",
    "verify:server": "node --check server/src/index.js && node --check server/src/middleware/auth.js",
    "verify:stack": "npm run verify:server && npm run lint --prefix web && npm run typecheck --prefix web && npm run build --prefix web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "firebase": "^12.11.0"
  }
}
